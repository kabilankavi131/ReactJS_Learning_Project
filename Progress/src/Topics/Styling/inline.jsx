import React, { Component } from "react";

export class inline extends Component {
  render() {
    const colors = {color:"yellow"};
    return (
      <div>
        <h1 style={colors}>Welcome Kabilan</h1>
      </div>
    );
  }
}

export default inline;
